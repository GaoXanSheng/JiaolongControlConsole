export enum ResultState {
	ON,
	OFF,
	Unknow = 255
}

export enum eumPerformaceMode {
	GamingMode,
	RampageMode,
	OfficeMode,
	Unknow = 255
}

export enum eumGPUMode {
	DiscreteMode,
	HybridMode,
	Unknow = 255
}

export enum eumCPUPower {
	CloseState,
	OpenState,
	Unknow = 255
}
