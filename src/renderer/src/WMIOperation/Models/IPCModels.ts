export enum ResultState {
  OFF,
  ON,
  Unknow = 255
}

export enum eumPerformaceMode {
  GamingMode,
  RampageMode,
  OfficeMode,
  Unknow = 255
}

export enum eumGPUMode {
  DiscreteMode,
  HybridMode,
  Unknow = 255
}

export enum eumCPUPower {
  CloseState = 0,
  OpenState,
  Unknow = 255
}
