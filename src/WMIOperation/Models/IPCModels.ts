export enum ResultState {
    OFF,
    ON
}

export enum eumPerformaceMode {
    GamingMode,
    RampageMode,
    OfficeMode,
    Unknow = 255
}
export enum eumGPUMode
{
    HybridMode,
    DiscreteMode,
    Unknow = 255
}
export enum eumCPUPower
{
    CloseState = 0,
    OpenState,
    Unknow = 255
}